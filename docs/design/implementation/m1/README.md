# M1 implementation screenshots

Current Goal Tracker React application captures used for the M2.1 visual-conformance review.
Mobile captures use a 390–392px viewport matching the mapped references.

- `auth-sign-in.png`
- `auth-create-account.png`
- `auth-registration-disabled.png`
- `auth-password-recovery.png`
- `account-settings.png`

The sign-up password field uses an accessible icon-only visibility control and a four-segment
strength indicator. Deployment capability states remain real application states rather than static
mockup panels.
