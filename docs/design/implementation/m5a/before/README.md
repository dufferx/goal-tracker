# M5A Before Baseline

These PNGs capture the real React implementation before M5A responsive changes. They are diagnostic
evidence, not approved design references.

- Capture date: 2026-08-04
- Mobile viewport: 390×844
- Desktop dashboard/detail/history/settings viewport: 1280×900
- Desktop create/simulator viewport: 1280×1000
- Data source: development-only deterministic `__design` fixtures
- Approved comparison sources: `docs/design/reference/m5/` and the mapped M1–M4 mobile references

The baseline confirms that dashboard/settings use a basic desktop shell while goal detail, creation,
history, and simulator remain independent mobile-width canvases. See
`docs/design/M5A_Surface_Matrix.md` for the full gap and capture mapping.
